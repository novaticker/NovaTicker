workflows:
  build-app:
    name: Build APK
    instance_type: mac_mini
    environment:
      flutter: stable
    scripts:
      - flutter pub get
      - flutter build apk --release
    artifacts:
      - build/app/outputs/flutter-apk/app-release.apk
